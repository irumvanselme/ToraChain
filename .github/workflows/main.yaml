name: main-ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  auth:
    name: auth-ci
    uses: ./.github/workflows/auth-ci.yaml

  backend:
    name: backend-ci
    uses: ./.github/workflows/backend-ci.yaml
