{
  "name": "@tora-chain/auth",
  "module": "app/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch app/server.ts",
    "start": "bun run app/server.ts",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "health-check": "bun run scripts/health-check.ts",
    "create-admin": "bun run scripts/create-admin.ts",
    "gen:logos": "bun run scripts/gen-logos.ts",
    "generate:voters": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bunx @better-auth/cli@latest generate --config app/auth/voters.ts --output migrations/voters.sql -y",
    "generate:admins": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bunx @better-auth/cli@latest generate --config app/auth/admins.ts --output migrations/admins.sql -y",
    "generate:auditors": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bunx @better-auth/cli@latest generate --config app/auth/auditors.ts --output migrations/auditors.sql -y",
    "generate:all": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bun run generate:voters && bun run generate:admins && bun run generate:auditors",
    "migrate:voters": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bunx @better-auth/cli@latest migrate --config app/auth/voters.ts -y",
    "migrate:admins": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bunx @better-auth/cli@latest migrate --config app/auth/admins.ts -y",
    "migrate:auditors": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bunx @better-auth/cli@latest migrate --config app/auth/auditors.ts -y",
    "migrate:core": "RUNNING_DB_MIGRATIONS_SCRIPTS=true bun run scripts/migrate-core.ts",
    "migrate:all": "bun run migrate:voters && bun run migrate:admins && bun run migrate:auditors && bun run migrate:core"
  },
  "dependencies": {
    "@elysia/html": "^1.4.1",
    "@elysiajs/cors": "^1.4.2",
    "@elysiajs/openapi": "^1.4.0",
    "@tora-chain/be-common": "workspace:*",
    "@tora-chain/configs": "workspace:*",
    "better-auth": "^1.6.14",
    "elysia": "^1.4.28",
    "pg": "^8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "latest",
    "@types/pg": "^8.20.0",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "prettier": "^3.8.4",
    "typescript-eslint": "^8.59.2",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
