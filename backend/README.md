# ToraChain Elections API (`backend`)

Bun + Elysia + Drizzle service implementing the elections/voting API
in [`prompt.md`](./prompt.md). Built on `@tora-chain/be-common` (`Database`, `Logger`, `config`).
