{
  "name": "torachain",
  "workspaces": [
    "apps/*",
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "make dev",
    "format": "bun run prettier --write .",
    "seed": "bun run scripts/seed.ts"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@tora-chain/configs": "workspace:*",
    "@types/bun": "latest",
    "concurrently": "^9.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "prettier": "^3.8.3"
  }
}
