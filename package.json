{
  "name": "torachain",
  "workspaces": [
    "backend",
    "admin-fe",
    "blockchain",
    "auth",
    "packages/*"
  ],
  "scripts": {
    "format": "bun run prettier --write .",
    "seed": "bun run scripts/seed.ts"
  },
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "prettier": "^3.8.3"
  }
}
